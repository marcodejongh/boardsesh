import React from 'react'
import { CheckOutlined } from '@ant-design/icons';
import { message } from 'antd';

const TickClimbButton = () => {
  return (<CheckOutlined onClick={()=> message.info('TODO: Implement')} />)
}

export default TickClimbButton;